# 2026-09-05 八图 V2 测试与优化链条

本文只记录本次真实运行和可复现验证。协议可解析、XML 合法、视觉内容完整、排版样式正确是不同结论，不能相互替代。实现与运行命令见 [服务端 CLI](server_pipeline.md)。

## 输入与实验条件

- 通过 `ssh wsl`，工作目录 `~/workspace/project/capture2doc/server`。
- 输入为用户提供的 `tests/image-test-group-1/image-1.jpg` 至 `image-8.jpg`，严格按 1 → 8 处理；每张实际解码尺寸均为 800×600。
- 图片为手机拍摄屏幕上的文档；包含工具栏、代码示意、表格、粗体与颜色标记，部分图片内容重叠，部分边缘被裁切。
- 两轮 V2 都复用旧基线八份正常 stop 的 OCR；核验原图/派生图身份、Paddle 配置、快照指纹及原始响应内容，没有重跑 OCR。
- Qwen3.5-9B，fp8_per_channel，16K context、8K 最大输出、640 MiB KV cache、单请求、thinking 关闭。硬件 RTX 4070 Ti SUPER，vLLM 0.28.0。
- 照片、完整正文、原始模型响应和本机绝对路径不提交 Git。可交付的完整 JSON/XML/逐块报告保存在本地与 WSL 的 `.cache/documents/`。

## 已验证的优化链条

### 1. 旧 V1：结构错误或 OCR 相似度失败会阻断整个文档

保留目录 `image-test-group-1-20260905/result`。第 1 张图累计六次尝试，已提交块为 0；错误包括 `blockquote` 内嵌 `pre`，以及 XML 已合法但被 OCR 字符覆盖率硬门槛拒绝。该基线没有完成八张图。

V2 改为独立草稿、逐块校验及最多五轮局部修复；耗尽时降级当前块，后续图片继续。字符对齐只保留诊断，不参与硬性覆盖率判定。

### 2. V2 普通 JSON：处理完八张，但外层语法错误使四张整图降级

提交 `03a7160`，目录 `image-test-group-1-v2-20260905/result`。八张均已提交，耗时 627.24 s，输出 27 块：23 块 ok、4 块 fallback、0 块 unresolved。最终 XML 合法，退出码 3，待复核。

第 2、3、6、7 张均因 `JSON_INVALID` 不能恢复候选列表，按既定兜底规则仅一次保留整图 OCR。四次响应均为正常 stop，说明 stop 本身不能保证外层协议完整。本轮没有进入 block 修复调用；程序没有补写 JSON/ XML 结束符。

| 图片 | 候选/降级块数 | 结果 | 主请求 tokens | 请求耗时 |
|---|---:|---|---:|---:|
| image-1 | 5 | 全部 ok | 4062 | 61.19 s |
| image-2 | 1 | 整图 OCR fallback | 5856 | 92.79 s |
| image-3 | 1 | 整图 OCR fallback | 7559 | 59.46 s |
| image-4 | 4 | 全部 ok | 7795 | 71.19 s |
| image-5 | 7 | 全部 ok | 5548 | 65.27 s |
| image-6 | 1 | 整图 OCR fallback | 6020 | 77.64 s |
| image-7 | 1 | 整图 OCR fallback | 7183 | 72.44 s |
| image-8 | 7 | 全部 ok | 7219 | 51.32 s |

Qwen 加载 66.06 s，卸载 0.81 s；全局显存峰值 15749 MiB，卸载后 1003 MiB，清理门禁通过。八次实际 prompt token 数均与 processor 预检相同。

对照图片已确认：image-1 右侧代码框没有进入输出；原图的除号被写成乘号，数字和代码示意有误识别。整图 OCR 降级还带回了“分享、编辑”等工具栏文字。27 块中仅有 2 个 b、没有颜色 span；标签计数只是现象，不是样式准确率。

针对四次相同协议失败，下一实验增加 `response_format=json_schema` 约束 action、blocks 和字段类型，内部 XML 仍由真实校验器检查。接口已对照安装版本及 [vLLM 官方文档](https://docs.vllm.ai/en/stable/features/structured_outputs/) 核验。保持模型参数和样式提示词不变；同时补齐错误字段类型的局部恢复、表格纯文本行间隔及截断 OCR 缺失位置的程序测试。

### 3. V2 JSON Schema 基线：协议成功，视觉质量仍需改进

提交 `7e4fdf1`，目录 `image-test-group-1-v2-schema-20260905/result`。八张完整处理，耗时 463.94 s，输出 64 块，全部 ok，无 fallback、unresolved 或自然触发的修复调用。各图块数为 5、4、9、8、7、16、8、7。八次 prompt 计数与实际 usage 相符，显存清理通过。完成后的 resume 耗时 0.15 s，没有新增模型调用或重复提交，XML 哈希不变。

相比普通 JSON 基线，四张整图协议降级消失；本次单次运行耗时减少 26.0%，不能据此推断稳定吞吐提升。64 块中有 18 个 b，仍没有颜色 span。视觉复核发现右侧频谱图和数据流步骤仍遗漏、代码缩进仍丢失、部分算术符号误识别，image-7/8 及非相邻图片的重叠正文重复。`needs_review=false` 只代表自动诊断未触发；`semantic_fidelity_verified=false`，不是人工质量验收。

独立故障注入 `image-test-group-1-repair-probe-20260905` 将一个真实 pre 放入非法 blockquote；模型一次修复将其拆成三块，同时复制了旁边已经通过的段落和表格。四个正常邻居本身没有被修改，但最终由五块变七块，出现重复。该注入不能计入八图自然错误率。

## 后续五轮迭代

以上两次 V2 八图运行作为基线，不计入此后的五轮。每轮使用独立输出目录，保留原始请求、响应、草稿、错误、提交状态与逐块报告；复用身份和配置一致的 OCR。视觉锚点由原图人工审阅建立，抽样检查内容、强调样式与重叠，标签数量不作为正确率。

### 第 1 轮：结构修复保护内容与旧尾块

针对真实故障注入发现的重复，在替换草稿之前检查整个目标组的文字、顺序和重复次数；保护失败块独立保存的文字，并单独保护代码空白。拒绝删字、扩写、复制邻居的 patch，保留原草稿与已消耗预算。旧尾块必须完整位于第一个替换块开头；不可分辨新增后缀时禁用 OCR 兜底，避免把旧尾再次插入正文。

此轮保持主生成提示词和模型参数不变；八图回归用于确认链路没有退化，另跑同一个真实修复注入以测量守恒门禁效果。运行结果待完成后补充。
