# 后端首版验收记录

实现阶段：本地 415 项回归通过，包括 22 项新增服务/真实 TCP SSE 合约测试。模型在合约测试中使用替身，XML 校验、文件检查点、SQLite 和网络传输是真实实现。公网及 GPU 验收进行中，完成后补充具体证据。

覆盖：创建/上传/finalize 幂等和冲突，C/A 页序与排除 B，A/C 先发布及 B 中间插入，OCR 空/截断仍完成，五次预算耗尽继续后图，尾块版本恢复，outbox 提交后确认前崩溃去重，断线补发、两连接限制、多设备及令牌撤销，GPU 锁等待与清理失败门禁，修复中断恢复不重置预算。

部署修正：OpenResty 正则含 `{32}` 必须加引号；systemd worker 显式配置虚拟环境 PATH，否则 `vllm` 可执行文件无法发现。首个公网任务 `9bc3ee486223418aa5013e7f47f135f2` 在模型启动前失败，产物保留，未伪装完成。
